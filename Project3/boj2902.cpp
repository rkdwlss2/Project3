/*#include <iostream>
#include <string>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	string str;
	cin >> str;
	string answer = "";
	for (int i = 0; i < str.size(); i++) {
		char now = str[i];
		if (now >= 'A' && now <= 'Z') {
			answer += now;
		}
	}
	cout << answer << '\n';
}*/